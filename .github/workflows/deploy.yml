name: "Proxmox Terraform Deploy"

on:
  push:
    branches:
      - main

jobs:
  terraform:
    name: "Terraform Apply"
    runs-on: self-hosted # <--- Forces the job to run inside your home LXC
    
    env:
      # Map GitHub secrets to environment variables that Terraform recognizes
      TF_VAR_pm_api_url: ${{ secrets.PM_API_URL }}
      TF_VAR_pm_api_token_id: ${{ secrets.PM_API_TOKEN_ID }}
      TF_VAR_pm_api_token_secret: ${{ secrets.PM_API_TOKEN_SECRET }}
      TF_VAR_container_root_password: ${{ secrets.TF_VAR_container_root_password }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # FIX: Corrected the malformed URL and fixed the missing curly brace syntax
      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="address=https://github.com{{ github.repository }}/actions/workflows/terraform-state" \
            -backend-config="username=${{ github.actor }}" \
            -backend-config="password=${{ secrets.GITHUB_TOKEN }}"

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        run: terraform apply -auto-approve

